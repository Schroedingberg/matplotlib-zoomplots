import os
from setuptools import setup
import setuptools_git as setupgit 


setup(
    name = "matplotlib-zoom",
    packages = ["zoom"],
    version="0.0",
    description="A matplotlib interface for generating plots with zoomed subplots"
)
